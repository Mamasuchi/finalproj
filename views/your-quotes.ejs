<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Quotes</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;500&family=Roboto:wght@300;500&display=swap" rel="stylesheet">
  <style>
    body {
      background-color: #FFFAE3;
      font-family: 'Roboto', sans-serif;
    }
    .container {
      margin-top: 50px;
    }
    .heading {
      text-align: center;
      font-family: 'Quicksand', sans-serif;
      font-weight: 500;
      color: #FF6347;
      margin-bottom: 30px;
    }
    .quote-card {
      background-color: #fff9e6;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
      margin-bottom: 20px;
      transition: transform 0.2s, box-shadow 0.2s;
    }
    .quote-card:hover {
      transform: translateY(-5px);
      box-shadow: 0px 6px 10px rgba(0, 0, 0, 0.2);
    }
    .quote-text {
      font-size: 1.2rem;
      font-weight: bold;
      color: #333;
    }
    .quote-author {
      font-style: italic;
      color: #555;
      margin-top: 10px;
    }
    .btn-delete {
      background-color: #FF6347;
      border: none;
      color: white;
      padding: 10px 20px;
      border-radius: 5px;
      font-size: 0.9rem;
      transition: background-color 0.3s, transform 0.2s;
    }
    .btn-delete:hover {
      background-color: #e63900;
      transform: scale(1.05);
    }
    .btn-back {
      background-color: #FFA07A;
      border: none;
      color: white;
      padding: 10px 30px;
      border-radius: 5px;
      font-size: 1rem;
      margin-top: 30px;
      transition: background-color 0.3s;
    }
    .btn-back:hover {
      background-color: #FF6347;
    }
  </style>
</head>
<body>
  <div class="container"><!-- Displays your quotes that youve created -->
    <h1 class="heading">Your Quotes</h1>
    <% if (quotes.length === 0) { %>
      <div class="alert alert-warning text-center" role="alert">
        You haven't added any quotes yet.
      </div>
    <% } else { %>
      <div class="row">
        <% quotes.forEach(quote => { %>
          <div class="col-md-6 col-lg-4">
            <div class="quote-card">
              <p class="quote-text">"<%= quote.quote %>"</p>
              <p class="quote-author">- <%= quote.author %></p>
              <!-- Delete quote -->
              <form action="/delete-quote" method="POST" class="d-inline">
                <input type="hidden" name="id" value="<%= quote.id %>">
                <button class="btn-delete" type="submit">Delete</button>
              </form>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>
    <div class="text-center">
      <a href="/" class="btn-back">Back to Home</a>
    </div>
  </div>
</body>
</html>
