<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Favorite Quotes</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;500&display=swap" rel="stylesheet">
  <style>
    body {
      background-color: #006666; 
      color: #e0e0e0;
      font-family: 'Roboto', sans-serif;
    }
    .container {
      margin-top: 50px;
    }
    .heading {
      text-align: center;
      font-weight: 500;
      color: #FFCC00; 
      margin-bottom: 30px;
    }
    .quote-list {
      list-style-type: none;
      padding: 0;
    }
    .quote-item {
      background-color: #004d4d; 
      padding: 15px 20px;
      border-radius: 10px;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.2);
      margin-bottom: 15px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      transition: transform 0.2s, box-shadow 0.2s;
    }
    .quote-item:hover {
      transform: translateY(-5px);
      box-shadow: 0px 6px 10px rgba(0, 0, 0, 0.3);
    }
    .quote-text {
      font-size: 1.1rem;
      color: #FFCC00; 
    }
    .quote-author {
      font-style: italic;
      color: #e0e0e0; 
    }
    .btn-delete {
      background-color: #FF6347; 
      border: none;
      color: white;
      padding: 8px 15px;
      font-size: 0.9rem;
      border-radius: 5px;
      transition: background-color 0.3s;
    }
    .btn-delete:hover {
      background-color: #e63900; 
    }
    .btn-back {
      background-color: #003d3d; 
      border: none;
      color: white;
      padding: 10px 30px;
      border-radius: 5px;
      font-size: 1rem;
      margin-top: 30px;
      transition: background-color 0.3s;
      display: block;
      text-align: center;
    }
    .btn-back:hover {
      background-color: #004d4d; 
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="heading">Your Favorite Quotes</h1>
<!-- displays favorite quotes of user that has favorited -->
    <% if (favoriteQuotes.length === 0) { %>
      <p class="text-center">You have no favorite quotes yet. Start adding some!</p>
    <% } else { %>
      <ul class="quote-list">
        <% favoriteQuotes.forEach(quote => { %>
          <li class="quote-item">
            <span>
              <span class="quote-text">"<%= quote.quote %>"</span> 
              <span class="quote-author">- <%= quote.author || "Anonymous" %></span>
            </span>
            <form action="/delete-favorite/<%= quote.id %>" method="POST" class="d-inline">
                <!-- remove quote Button -->
              <button type="submit" class="btn-delete">Remove</button>
            </form>
          </li>
        <% }); %>
      </ul>
    <% } %>

    <a href="/" class="btn-back">Back to Home</a>
  </div>
</body>
</html>
