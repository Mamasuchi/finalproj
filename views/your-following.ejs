<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Users You're Following</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;500&family=Roboto:wght@300;500&display=swap" rel="stylesheet">
  <style>
    body {
      background-color: #F7F8FA; 
      font-family: 'Roboto', sans-serif;
    }
    .container {
      margin-top: 50px;
    }
    .heading {
      text-align: center;
      font-family: 'Quicksand', sans-serif;
      font-weight: 500;
      color: #00B8A9; 
      margin-bottom: 30px;
    }
    .empty-state {
      text-align: center;
      font-size: 1.2rem;
      color: #555;
      margin-top: 20px;
    }
    .user-list {
      list-style-type: none;
      padding: 0;
    }
    .user-list-item {
      background-color: #ffffff; 
      padding: 15px 20px;
      border-radius: 10px;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
      margin-bottom: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      transition: transform 0.2s, box-shadow 0.2s;
    }
    .user-list-item:hover {
      transform: translateY(-5px);
      box-shadow: 0px 6px 10px rgba(0, 0, 0, 0.2);
    }
    .user-link {
      text-decoration: none;
      font-size: 1.1rem;
      color: #4A90E2; 
      font-weight: 500;
    }
    .user-link:hover {
      color: #00B8A9; 
      text-decoration: underline;
    }
    .btn-unfollow {
      background-color: #2F3A3F; 
      border: none;
      color: white;
      padding: 8px 15px;
      font-size: 0.9rem;
      border-radius: 5px;
      transition: background-color 0.3s;
    }
    .btn-unfollow:hover {
      background-color: #333333; 
    }
    .btn-back {
      background-color: #4A90E2; 
      border: none;
      color: white;
      padding: 10px 30px;
      border-radius: 5px;
      font-size: 1rem;
      margin-top: 30px;
      transition: background-color 0.3s;
    }
    .btn-back:hover {
      background-color: #00B8A9; 
    }
  </style>
</head>
<body>
  <div class="container"><!-- Displays users you are following-->
    <h1 class="heading">Users You're Following</h1>
    <% if (following.length === 0) { %>
      <p class="empty-state">You're not following anyone yet.</p>
    <% } else { %>
      <ul class="user-list">
        <% following.forEach(f => { %>
          <li class="user-list-item">
            <a href="/profile/<%= f.followed %>" class="user-link"><%= f.followed %></a>
            <!-- Unfollow Button -->
            <form action="/unfollow-user" method="POST" class="d-inline">
              <input type="hidden" name="followed_username" value="<%= f.followed %>">
              <button type="submit" class="btn-unfollow">Unfollow</button>
            </form>
          </li>
        <% }); %>
      </ul>
    <% } %>
    <div class="text-center">
      <a href="/" class="btn-back">Back to Home</a>
    </div>
  </div>
</body>
</html>
